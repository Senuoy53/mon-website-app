import styled from "styled-components";

const SlideImageWrapper = styled.img`
  max-width: 100%;

  width: auto;
  height: auto;
  padding: 10px;
  border-radius: 5px;
`;

export default SlideImageWrapper;
